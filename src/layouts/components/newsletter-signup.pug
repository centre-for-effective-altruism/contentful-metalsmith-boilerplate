- var text = showText === false ? false : true;
.newsletter-signup
	if text
		.newsletter-signup-call-to-action-wrapper
			p.newsletter-signup-call-to-action Subscribe to the newsletter:
	.newsletter-signup-form-wrapper
		// - Add a form action
		form.form.newsletter-signup-form(action="" method="post" target="_blank" novalidate)
			.input-group
				label(for="newsletter-signup-email" class="control-label sr-only" aria-hidden="true") Email Address (required)
				input.form-control.newsletter-signup-email(placeholder='Email address' name="EMAIL")
				span.input-group-btn
					button.btn.btn-primary.newsletter-signup-subscribe Subscribe
			.clearfix
				.newsletter-signup-result
				//- Mailchimp anti-bot code, replace the name field below with the one generated by mailchimp
					div(style="position: absolute; left: -5000px;" aria-hidden="true")
						input(type="text" name="XXXXXX" tabindex="-1" value="")
				input(type="hidden" value=site.domain name="SOURCE")
script.
	(function(){
		var d = document.getElementsByClassName("newsletter-signup-subscribe");
		for (var i = 0; i < d.length; i++) {
			// disable while javascript handler is loading, will be re-enabled by the script once loaded
			// see ./src/scripts/lib/newsletter.js
			d[i].className += " disabled";
		}
	}());